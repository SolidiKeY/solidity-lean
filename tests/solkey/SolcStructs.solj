// SolcStructs — ported from solkey solc/SolcStructs.sol.
// Generated by scripts/solkey-port.mjs; do not edit by hand.

// SolcStructs.deleteMemberThroughStorageAlias
< data1@@Simple.value = 2;
  Simple storage x = data1@@Simple;
  x@Simple.value = 4;
  delete x@Simple.value;
  assert((data1@@Simple.value == 0)) > (true)

// SolcStructs.deleteStructLeavesNeighbours
< neighbourBefore@@uint = 23;
  neighbourAfter@@uint = 17;
  triple@@Triple.x = 42;
  triple@@Triple.y = 42;
  triple@@Triple.z = 42;
  delete triple@@Triple;
  assert((triple@@Triple.x == 0));
  assert((triple@@Triple.y == 0));
  assert((triple@@Triple.z == 0));
  assert((neighbourBefore@@uint == 23));
  assert((neighbourAfter@@uint == 17)) > (true)

// SolcStructs.deleteStructResetsArrayLength
< (withArray@@WithArray.items).push();
  (withArray@@WithArray.items).push();
  delete withArray@@WithArray;
  uint len = withArray@@WithArray.items.length;
  assert((len == 0)) > (true)

// SolcStructs.deleteStructInMapping
< campaigns@@SimpleMap[0].value = 2;
  delete campaigns@@SimpleMap[0];
  assert((campaigns@@SimpleMap[0].value == 0)) > (true)

// SolcStructs.deleteEntryKeepsOtherKeys
< campaigns@@SimpleMap[0].value = 2;
  campaigns@@SimpleMap[1].value = 3;
  delete campaigns@@SimpleMap[0];
  assert((campaigns@@SimpleMap[0].value == 0));
  assert((campaigns@@SimpleMap[1].value == 3)) > (true)

// SolcStructs.structCopyViaMemoryLocal
< source@@Pair.a = 1;
  source@@Pair.b = 2;
  Pair memory mv = source@@Pair;
  target@@Pair = mv@Pair;
  assert((target@@Pair.a == 1));
  assert((target@@Pair.b == 2)) > (true)

// SolcStructs.structCopyFromStorageIsDeep
< source@@Pair.a = 13;
  source@@Pair.b = 14;
  target@@Pair = source@@Pair;
  source@@Pair.a = 20;
  source@@Pair.b = 21;
  assert((target@@Pair.a == 13));
  assert((target@@Pair.b == 14)) > (true)

// SolcStructs.structArrayElementCopy
< (pairs1@@PairArray).push();
  (pairs2@@PairArray).push();
  pairs1@@PairArray[0].a = 4;
  pairs1@@PairArray[0].b = 5;
  Pair storage src = pairs1@@PairArray[0];
  pairs2@@PairArray[0] = src@Pair;
  assert((pairs2@@PairArray[0].a == 4));
  assert((pairs2@@PairArray[0].b == 5)) > (true)

// SolcStructs.structArrayElementCopyMemberwise
< (pairs1@@PairArray).push();
  (pairs2@@PairArray).push();
  pairs1@@PairArray[0].a = 4;
  pairs1@@PairArray[0].b = 5;
  Pair storage src = pairs1@@PairArray[0];
  Pair storage dst = pairs2@@PairArray[0];
  dst@Pair.a = src@Pair.a;
  dst@Pair.b = src@Pair.b;
  assert((pairs2@@PairArray[0].a == 4));
  assert((pairs2@@PairArray[0].b == 5)) > (true)
