// SolcExpressions — ported from solkey solc/SolcExpressions.sol.
// Generated by scripts/solkey-port.mjs; do not edit by hand.

// SolcExpressions.postIncrementYieldsOldValue
< uint a = 6;
  uint v_p = a++;
  assert((v_p == 6));
  assert((a == 7)) > (true)

// SolcExpressions.preIncrementYieldsNewValue
< uint a = 6;
  uint q = ++a;
  assert((q == 7));
  assert((a == 7)) > (true)

// SolcExpressions.incDecChainOnLocal
< uint a = 6;
  uint r = a;
  uint v_p = a++;
  r = r + v_p * 16;
  uint q = ++a;
  r = r + q * 256;
  assert((a == 8));
  assert((r == 2150)) > (true)

// SolcExpressions.incDecChainOnStateVariable
< counter@@uint = 3;
  uint v_p = counter@@uint++;
  uint r = v_p * 4096;
  uint q = ++counter@@uint;
  r = r + q * 65536;
  assert((counter@@uint == 5));
  assert((r == 339968)) > (true)

// SolcExpressions.postDecrementYieldsOldValue
< uint a = 6;
  uint v_p = postdec(a);
  assert((v_p == 6));
  assert((a == 5)) > (true)

// SolcExpressions.preDecrementYieldsNewValue
< uint a = 6;
  uint q = predec(a);
  assert((q == 5));
  assert((a == 5)) > (true)

// SolcExpressions.compoundAssignOnLocal
< uint a = 6;
  uint r = a;
  r += a * 16;
  assert((r == 102)) > (true)

// SolcExpressions.bareIncrementOnLocal
< uint a = 6;
  a++;
  assert((a == 7)) > (true)

// SolcExpressions.bareIncrementOnStateVariable
< counter@@uint = 6;
  counter@@uint++;
  assert((counter@@uint == 7)) > (true)

// SolcExpressions.ternaryTrueLiteral
< uint r = true ? 5 : 10;
  assert((r == 5)) > (true)

// SolcExpressions.ternaryFalseLiteral
< uint r = false ? 5 : 10;
  assert((r == 10)) > (true)

// SolcExpressions.ternaryNestedOuterHigh — concretized: solkey proves this for all parameters; ported at x = 1001
< uint x = 1001;
  uint d = (x > 100) ? ((x > 1000) ? 1000 : 100) : ((x > 50) ? 50 : 10);
  assert((d == 1000)) > (true)

// SolcExpressions.ternaryNestedOuterLow — concretized: solkey proves this for all parameters; ported at x = 500
< uint x = 500;
  uint d = (x > 100) ? ((x > 1000) ? 1000 : 100) : ((x > 50) ? 50 : 10);
  assert((d == 100)) > (true)

// SolcExpressions.ternaryNestedInnerHigh — concretized: solkey proves this for all parameters; ported at x = 80
< uint x = 80;
  uint d = (x > 100) ? ((x > 1000) ? 1000 : 100) : ((x > 50) ? 50 : 10);
  assert((d == 50)) > (true)

// SolcExpressions.ternaryNestedInnerLow — concretized: solkey proves this for all parameters; ported at x = 40
< uint x = 40;
  uint d = (x > 100) ? ((x > 1000) ? 1000 : 100) : ((x > 50) ? 50 : 10);
  assert((d == 10)) > (true)

// SolcExpressions.exponentiationIsRightAssociative
< uint r = 2 ** 3 ** 2;
  assert((r == 512)) > (true)

// SolcExpressions.exponentiationZeroBaseZeroExponent
< uint r = 0 ** 0;
  assert((r == 1)) > (true)

// SolcExpressions.exponentiationZeroExponent
< uint r = 7 ** 0;
  assert((r == 1)) > (true)

// SolcExpressions.exponentiationSignedBaseEvenExponent — concretized: solkey proves this for all parameters; ported at b = minusTwo
< int minusTwo = -2;
  int b = minusTwo;
  int even = b ** 2;
  assert((even == 4)) > (true)

// SolcExpressions.exponentiationSignedBaseOddExponent — concretized: solkey proves this for all parameters; ported at b = minusTwo
< int minusTwo = -2;
  int b = minusTwo;
  int odd = b ** 3;
  int expectedOdd = -8;
  assert((odd == expectedOdd)) > (true)
